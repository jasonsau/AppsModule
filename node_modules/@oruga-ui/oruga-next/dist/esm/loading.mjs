import { createVNode, render } from 'vue';
import { s as script } from './Loading-2c4345ed.mjs';
export { s as OLoading } from './Loading-2c4345ed.mjs';
import { VueInstance } from './config.mjs';
import { merge } from './helpers.mjs';
import { b as registerComponent, a as registerComponentProgrammatic } from './plugins-0d853d1f.mjs';
import { I as InstanceRegistry } from './InstanceRegistry-1e116bd6.mjs';
import './Icon-e7ad13c9.mjs';
import './ssr-85c76d50.mjs';

let localVueInstance;
let instances = new InstanceRegistry();
const LoadingProgrammatic = {
    open(params) {
        const defaultParam = {
            programmatic: { instances }
        };
        const propsData = merge(defaultParam, params);
        propsData.promise = new Promise((p1, p2) => {
            propsData.programmatic.resolve = p1;
            propsData.programmatic.reject = p2;
        });
        const app = localVueInstance || VueInstance;
        const vnode = createVNode(script, propsData);
        vnode.appContext = app._context;
        render(vnode, document.createElement('div'));
        return vnode.component.proxy;
    },
    closeAll() {
        instances.walk((entry) => {
            entry.close(...arguments);
        });
    }
};
var index = {
    install(app) {
        localVueInstance = app;
        registerComponent(app, script);
        registerComponentProgrammatic(app, 'loading', LoadingProgrammatic);
    }
};

export { LoadingProgrammatic, index as default };
